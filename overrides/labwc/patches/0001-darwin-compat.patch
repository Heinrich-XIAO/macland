diff --git a/clients/labnag.c b/clients/labnag.c
index 31ac4e9d..c9ff1712 100644
--- a/clients/labnag.c
+++ b/clients/labnag.c
@@ -16,6 +16,13 @@
 #include <stdio.h>
 #include <stdint.h>
 #include <stdlib.h>
+#include <time.h>
+#ifdef __APPLE__
+struct itimerspec {
+	struct timespec it_interval;
+	struct timespec it_value;
+};
+#endif
 #ifdef __FreeBSD__
 #include <sys/event.h> /* For signalfd() */
 #endif
@@ -23,7 +30,6 @@
 #include <sys/signalfd.h>
 #include <sys/timerfd.h>
 #include <sys/wait.h>
-#include <time.h>
 #include <unistd.h>
 #include <wayland-cursor.h>
 #include <wlr/util/log.h>
diff --git a/src/config/session.c b/src/config/session.c
index 515b6803..9886bb1c 100644
--- a/src/config/session.c
+++ b/src/config/session.c
@@ -179,9 +179,14 @@ env_dir_cleanup:
 static void
 backend_check_drm(struct wlr_backend *backend, void *is_drm)
 {
+#if WLR_HAS_DRM_BACKEND
 	if (wlr_backend_is_drm(backend)) {
 		*(bool *)is_drm = true;
 	}
+#else
+	(void)backend;
+	(void)is_drm;
+#endif
 }
 
 static bool
diff --git a/src/input/cursor.c b/src/input/cursor.c
index ababb2cd..bbc98188 100644
--- a/src/input/cursor.c
+++ b/src/input/cursor.c
@@ -900,6 +900,7 @@ preprocess_cursor_motion(struct seat *seat, struct wlr_pointer *pointer,
 
 static double get_natural_scroll_factor(struct wlr_input_device *wlr_input_device)
 {
+#if WLR_HAS_LIBINPUT_BACKEND
 	if (wlr_input_device_is_libinput(wlr_input_device)) {
 		struct libinput_device *libinput_device =
 			wlr_libinput_get_device_handle(wlr_input_device);
@@ -907,6 +908,9 @@ static double get_natural_scroll_factor(struct wlr_input_device *wlr_input_devic
 			return -1.0;
 		}
 	}
+#else
+	(void)wlr_input_device;
+#endif
 
 	return 1.0;
 }
diff --git a/src/input/keyboard.c b/src/input/keyboard.c
index 0171dcd0..3ce5373a 100644
--- a/src/input/keyboard.c
+++ b/src/input/keyboard.c
@@ -54,7 +54,12 @@ keyboard_reset_current_keybind(void)
 static void
 change_vt(struct server *server, unsigned int vt)
 {
+#if WLR_HAS_SESSION
 	wlr_session_change_vt(server->session, vt);
+#else
+	(void)server;
+	(void)vt;
+#endif
 }
 
 uint32_t
diff --git a/src/input/tablet-pad.c b/src/input/tablet-pad.c
index 230b6e6b..0a9bc2ca 100644
--- a/src/input/tablet-pad.c
+++ b/src/input/tablet-pad.c
@@ -17,6 +17,10 @@
 void
 tablet_pad_attach_tablet(struct seat *seat)
 {
+#if !WLR_HAS_LIBINPUT_BACKEND
+	(void)seat;
+	return;
+#else
 	/* reset all tablet - pad links */
 	struct drawing_tablet_pad *pad;
 	wl_list_for_each(pad, &seat->tablet_pads, link) {
@@ -56,6 +60,7 @@ tablet_pad_attach_tablet(struct seat *seat)
 			}
 		}
 	}
+#endif
 }
 
 static void
diff --git a/src/output.c b/src/output.c
index addb2706..326e1d80 100644
--- a/src/output.c
+++ b/src/output.c
@@ -508,10 +508,12 @@ handle_new_output(struct wl_listener *listener, void *data)
 	 * TODO: remove once labwc starts tracking 0.20.x and the fix has been merged.
 	 */
 #if LAB_WLR_VERSION_AT_LEAST(0, 19, 1)
+#if WLR_HAS_DRM_BACKEND
 	if (server->drm_lease_manager && wlr_output_is_drm(wlr_output)) {
 		wlr_drm_lease_v1_manager_offer_output(
 			server->drm_lease_manager, wlr_output);
 	}
+#endif
 #endif
 
 	/*
@@ -593,7 +595,11 @@ handle_new_output(struct wl_listener *listener, void *data)
 	 * it would result in no outputs being enabled at all. This check
 	 * might need tweaking if wlroots adds other output backends.
 	 */
-	if (rc.auto_enable_outputs || !wlr_output_is_drm(wlr_output)) {
+	bool output_is_drm = false;
+#if WLR_HAS_DRM_BACKEND
+	output_is_drm = wlr_output_is_drm(wlr_output);
+#endif
+	if (rc.auto_enable_outputs || !output_is_drm) {
 		configure_new_output(server, output);
 	}
 
diff --git a/src/seat.c b/src/seat.c
index 8aa395cf..85da4d2c 100644
--- a/src/seat.c
+++ b/src/seat.c
@@ -59,6 +59,7 @@ device_type_from_wlr_device(struct wlr_input_device *wlr_input_device)
 		break;
 	}
 
+#if WLR_HAS_LIBINPUT_BACKEND
 	if (wlr_input_device->type == WLR_INPUT_DEVICE_POINTER &&
 			wlr_input_device_is_libinput(wlr_input_device)) {
 		struct libinput_device *libinput_device =
@@ -68,6 +69,7 @@ device_type_from_wlr_device(struct wlr_input_device *wlr_input_device)
 			return LAB_LIBINPUT_DEVICE_TOUCHPAD;
 		}
 	}
+#endif
 
 	return LAB_LIBINPUT_DEVICE_NON_TOUCH;
 }
@@ -131,6 +133,11 @@ configure_libinput(struct wlr_input_device *wlr_input_device)
 	assert(input);  /* Check the caller remembered to set this! */
 
 	/* Set scroll factor to 1.0 for Wayland/X11 backends or virtual pointers */
+#if !WLR_HAS_LIBINPUT_BACKEND
+	input->scroll_factor = 1.0;
+	return;
+#endif
+
 	if (!wlr_input_device_is_libinput(wlr_input_device)) {
 		input->scroll_factor = 1.0;
 		return;
diff --git a/src/server.c b/src/server.c
index b8abc633..7907707d 100644
--- a/src/server.c
+++ b/src/server.c
@@ -196,6 +196,7 @@ handle_sigchld(int signal, void *data)
 static void
 handle_drm_lease_request(struct wl_listener *listener, void *data)
 {
+#if LAB_WLR_VERSION_AT_LEAST(0, 19, 1) && WLR_HAS_DRM_BACKEND
 	struct wlr_drm_lease_request_v1 *req = data;
 	struct wlr_drm_lease_v1 *lease = wlr_drm_lease_request_v1_grant(req);
 	if (!lease) {
@@ -203,6 +204,10 @@ handle_drm_lease_request(struct wl_listener *listener, void *data)
 		wlr_drm_lease_request_v1_reject(req);
 		return;
 	}
+#else
+	(void)listener;
+	(void)data;
+#endif
 }
 
 static bool
@@ -690,6 +695,7 @@ server_init(struct server *server)
 
 	session_lock_init(server);
 
+#if WLR_HAS_DRM_BACKEND
 	server->drm_lease_manager = wlr_drm_lease_v1_manager_create(
 		server->wl_display, server->backend);
 	if (server->drm_lease_manager) {
@@ -700,6 +706,9 @@ server_init(struct server *server)
 		wlr_log(WLR_DEBUG, "Failed to create wlr_drm_lease_device_v1");
 		wlr_log(WLR_INFO, "VR will not be available");
 	}
+#else
+	server->drm_lease_manager = NULL;
+#endif
 
 	server->output_power_manager_v1 =
 		wlr_output_power_manager_v1_create(server->wl_display);
