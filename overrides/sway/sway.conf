# Minimal macland nested config used for startup validation on macOS.
swaybg_command -
default_border pixel 1
focus_follows_mouse no
bar {
    position top
    status_command sh ../../../scripts/sway_status.sh
}
