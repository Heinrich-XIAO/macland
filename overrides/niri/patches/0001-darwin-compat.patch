diff --git a/src/backend/winit.rs b/src/backend/winit.rs
index beace1dc..7ab5c4aa 100644
--- a/src/backend/winit.rs
+++ b/src/backend/winit.rs
@@ -14,6 +14,7 @@ use smithay::output::{Mode, Output, PhysicalProperties, Subpixel};
 use smithay::reexports::calloop::LoopHandle;
 use smithay::reexports::wayland_protocols::wp::presentation_time::server::wp_presentation_feedback;
 use smithay::reexports::winit::dpi::LogicalSize;
+#[cfg(any(target_os = "linux", target_os = "freebsd", target_os = "dragonfly", target_os = "netbsd", target_os = "openbsd", docsrs))]
 use smithay::reexports::winit::platform::wayland::WindowAttributesExtWayland;
 use smithay::reexports::winit::window::Window;
 use smithay::wayland::presentation::Refresh;
@@ -42,8 +43,9 @@ impl Winit {
         let builder = Window::default_attributes()
             .with_inner_size(LogicalSize::new(1280.0, 800.0))
             // .with_resizable(false)
-            .with_title("niri")
-            .with_name("niri", "");
+            .with_title("niri");
+        #[cfg(any(target_os = "linux", target_os = "freebsd", target_os = "dragonfly", target_os = "netbsd", target_os = "openbsd", docsrs))]
+        let builder = builder.with_name("niri", "");
         let (backend, winit) = winit::init_from_attributes(builder)?;
 
         let output = Output::new(
diff --git a/src/niri.rs b/src/niri.rs
index d84c390a..7d666a7f 100644
--- a/src/niri.rs
+++ b/src/niri.rs
@@ -705,6 +705,7 @@ impl State {
 
         let config = Rc::new(RefCell::new(config));
 
+        let macland_headless = env::var_os("MACLAND_HEADLESS").is_some();
         let has_display = env::var_os("WAYLAND_DISPLAY").is_some()
             || env::var_os("WAYLAND_SOCKET").is_some()
             || env::var_os("DISPLAY").is_some();
@@ -712,6 +713,9 @@ impl State {
         let mut backend = if headless {
             let headless = Headless::new();
             Backend::Headless(headless)
+        } else if macland_headless {
+            let headless = Headless::new();
+            Backend::Headless(headless)
         } else if has_display {
             let winit = Winit::new(config.clone(), event_loop.clone())?;
             Backend::Winit(winit)
@@ -733,6 +737,14 @@ impl State {
         backend.init(&mut niri);
 
         let mut state = Self { backend, niri };
+        if macland_headless {
+            state
+                .backend
+                .headless()
+                .add_renderer()
+                .context("error initializing the headless backend renderer")?;
+            state.backend.headless().add_output(&mut state.niri, 1, (1280, 800));
+        }
 
         // Load the xkb_file config option if set by the user.
         state.load_xkb_file();
